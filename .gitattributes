* text=auto

# Shell scripts must use LF endings — CRLF breaks the shebang in Linux containers
*.sh text eol=lf
*.bash text eol=lf

# Docker files must use LF
Dockerfile text eol=lf
.dockerignore text eol=lf
docker-compose*.yml text eol=lf
